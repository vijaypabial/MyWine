<h1>Wines</h1>

 <% @wines.each do |wine|%>
     <p><%= image_tag wine.wine_image%></p>
     <p><%= wine.title%></p>
     <p><%= wine.variety%></p>
     <p><%= wine.description%></p>
     <p><%= wine.price%></p>
    <%end%>