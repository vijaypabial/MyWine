<h1>Wines</h1>

 <% @wines.each do |wine|%>
     <p><%= wine.title%></p>
     <p><%= image_tag("download.jpeg")%></p>
     <p><%= wine.variety%></p>
     <p><%= wine.description%></p>
     <p><%= wine.price%></p>
    <%end%>